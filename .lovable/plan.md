
The scan-food edge function already exists and `src/lib/scan.ts` already wraps it. I need to verify the existing prompt matches the requested one, then build the camera capture UI flow and wire it into QuickLogModal → ManualLog with prefilled values.

Let me check current state of relevant files first.
